{% extends 'base.html.twig' %}

{% block title %}Organisateur index{% endblock %}

{% block body %}
    <h1>Organisateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Mail</th>
                <th>Tel</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for organisateur in organisateurs %}
            <tr>
                <td>{{ organisateur.id }}</td>
                <td>{{ organisateur.name }}</td>
                <td>{{ organisateur.mail }}</td>
                <td>{{ organisateur.tel }}</td>
                <td>
                    <a href="{{ path('organisateur_show', {'id': organisateur.id}) }}">show</a>
                    <a href="{{ path('organisateur_edit', {'id': organisateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('organisateur_new') }}">Create new</a>
{% endblock %}
