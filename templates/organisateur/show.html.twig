{% extends 'base.html.twig' %}

{% block title %}Organisateur{% endblock %}

{% block body %}
    <h1>Organisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ organisateur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ organisateur.name }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ organisateur.mail }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ organisateur.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('organisateur_index') }}">back to list</a>

    <a href="{{ path('organisateur_edit', {'id': organisateur.id}) }}">edit</a>

    {{ include('organisateur/_delete_form.html.twig') }}
{% endblock %}
