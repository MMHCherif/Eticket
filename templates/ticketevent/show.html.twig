{% extends 'base.html.twig' %}

{% block title %}Ticketevent{% endblock %}

{% block body %}
    <h1>Ticketevent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticketevent.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ticketevent.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ ticketevent.price }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ ticketevent.quantity }}</td>
            </tr>
            <tr>
                <th>Stopsaleat</th>
                <td>{{ ticketevent.stopsaleat ? ticketevent.stopsaleat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ticketevent_index') }}">back to list</a>

    <a href="{{ path('ticketevent_edit', {'id': ticketevent.id}) }}">edit</a>

    {{ include('ticketevent/_delete_form.html.twig') }}
{% endblock %}
